node_modules/
.next/
__pycache__/
*.pyc
.env
.env.local
venv/
web/dev.log
.vercel

# Runtime state written by laptop_mode.py. Machine-specific and rewritten on
# every pass, so committing it just produces conflicts between machines.
scraper/.laptop_mode_state.json
scraper/.maxun_bridge_state.json

# Row-level dumps taken before a destructive migration, so a bad change can be
# undone by hand. Real production data, and the point of them is to exist on
# disk at the moment of the change - not to be version controlled.
_recovery/

# Data samples + security audit docs stay OUT of the repo (private data)
audit-output/scraped-data.csv
audit-output/network-endpoints.csv
security-audit/
